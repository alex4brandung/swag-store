# Swag Store API (required)
SWAG_API_BASE_URL=
SWAG_API_BYPASS_TOKEN=

# Site URL for metadata / OG images (optional — falls back to VERCEL_URL, then localhost:3000)
NEXT_PUBLIC_SITE_URL=

# Debug: logs per-request API timings in the server console when set to 1
SWAG_DEBUG_TIMINGS=
